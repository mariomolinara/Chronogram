<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for Chronogram.

  DEFAULT (production): cleartext HTTP traffic is FORBIDDEN. All network
  traffic must go over HTTPS towards the backend (context-path /chronogram).

  EXCEPTION (local development only): the domain-config below permits
  cleartext HTTP *only* towards local/dev hosts so the app can still be
  tested against a backend running on the developer machine or emulator.

  Dev hosts allowed:
    - localhost / 127.0.0.1  -> backend on the same device (adb reverse, etc.)
    - 10.0.2.2               -> host machine as seen from the Android emulator
    - 10.0.3.2               -> host machine as seen from the Genymotion emulator
    - 192.168.1.100          -> example LAN IP for a physical test device

  TO USE A DIFFERENT TEST HOST/IP: replace or add a <domain> entry below with
  your machine's LAN address (e.g. 192.168.x.y). Do NOT add public/production
  domains here — production must stay HTTPS-only.
-->
<network-security-config>
    <!-- Production default: no cleartext anywhere. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Local development exception: cleartext allowed only for these hosts. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">192.168.1.100</domain>
    </domain-config>
</network-security-config>
